name: dapur_atik
description: "Aplikasi Pre Order Dapur Atik"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
      sdk: flutter

  pdf: ^3.10.8
  printing: ^5.12.0

  #URL LAUNCHER
  url_launcher: ^6.3.0

  cupertino_icons: ^1.0.8

  # FIREBASE
  firebase_core: ^3.6.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.3.0

  # IMAGE
  image_picker: ^1.1.2

  # FONT
  google_fonts: ^6.2.1

  # CHART
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true